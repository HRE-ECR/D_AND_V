import{createClient}from'@supabase/supabase-js';const u=import.meta.env.VITE_SUPABASE_URL,k=import.meta.env.VITE_SUPABASE_PUBLISHABLE_KEY||import.meta.env.VITE_SUPABASE_ANON_KEY;if(!u||!k)console.warn('Supabase environment variables are missing.');export const supabase=createClient(u||'https://placeholder.supabase.co',k||'placeholder',{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true}});
